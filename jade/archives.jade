extends ./base.jade

block content
  each post in posts
    article
      header
        h1
          a(href="#{baseURL(post.link)}") #{post.title}
        .meta
          .key Update:
          .value #{post.modificationTime}
          .key Tags:
          .value #{post.tags.join(', ') || 'Null'}
